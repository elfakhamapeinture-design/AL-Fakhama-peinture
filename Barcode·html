<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>مولّد ملصقات الباركود — طباعة حرارية</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;900&family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/JsBarcode/3.11.5/JsBarcode.all.min.js"></script>
<style>
  :root{
    --ink:#141821;
    --panel:#1c212c;
    --panel-2:#232935;
    --line:#333b4a;
    --paper:#fbf9f4;
    --paper-line:#d9d4c8;
    --amber:#ff7a33;
    --amber-dim:#c75a1f;
    --mint:#3ddc97;
    --muted:#8a92a3;
    --text:#eef0f4;
    --danger:#ff5c5c;
  }
  *{box-sizing:border-box;}
  html,body{margin:0;padding:0;}
  body{
    background:var(--ink);
    color:var(--text);
    font-family:'Tajawal',sans-serif;
    min-height:100vh;
    padding:28px 18px 60px;
  }
  .wrap{max-width:1080px;margin:0 auto;}

  header.top{
    display:flex;align-items:center;justify-content:space-between;
    margin-bottom:26px;flex-wrap:wrap;gap:14px;
  }
  .brand{display:flex;align-items:center;gap:12px;}
  .brand .mark{
    width:42px;height:42px;border-radius:9px;
    background:linear-gradient(155deg,var(--amber),var(--amber-dim));
    display:flex;align-items:center;justify-content:center;
    font-family:'JetBrains Mono',monospace;font-weight:700;font-size:13px;color:#1c1204;
    flex-shrink:0;
  }
  .brand h1{font-size:19px;margin:0;font-weight:900;letter-spacing:.2px;}
  .brand p{margin:2px 0 0;font-size:12.5px;color:var(--muted);}

  .status{
    display:flex;align-items:center;gap:8px;
    font-family:'JetBrains Mono',monospace;font-size:11.5px;
    background:var(--panel);border:1px solid var(--line);
    padding:8px 14px;border-radius:999px;color:var(--muted);
  }
  .dot{width:8px;height:8px;border-radius:50%;background:var(--mint);box-shadow:0 0 8px var(--mint);}

  .grid{
    display:grid;
    grid-template-columns:380px 1fr;
    gap:22px;
  }
  @media (max-width:860px){ .grid{grid-template-columns:1fr;} }

  .panel{
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:16px;
    padding:22px;
  }
  .panel h2{
    font-size:12px;text-transform:uppercase;letter-spacing:1.2px;
    color:var(--amber);margin:0 0 18px;font-weight:700;
    display:flex;align-items:center;gap:8px;
  }
  .panel h2::before{content:'';width:14px;height:2px;background:var(--amber);display:inline-block;}

  label.field{display:block;margin-bottom:16px;}
  label.field span{
    display:block;font-size:12.5px;color:var(--muted);margin-bottom:6px;font-weight:500;
  }
  input[type=text], input[type=number], select{
    width:100%;
    background:var(--panel-2);
    border:1px solid var(--line);
    color:var(--text);
    padding:11px 12px;
    border-radius:9px;
    font-family:'Tajawal',sans-serif;
    font-size:14.5px;
    outline:none;
    transition:border-color .15s;
  }
  input[type=text]:focus, input[type=number]:focus, select:focus{border-color:var(--amber);}
  input#barcodeValue, input#skuValue{font-family:'JetBrains Mono',monospace;letter-spacing:.5px;}

  .row2{display:grid;grid-template-columns:1fr 1fr;gap:12px;}

  .btn-row{display:flex;gap:10px;margin-top:6px;}
  button{
    font-family:'Tajawal',sans-serif;font-weight:700;font-size:14px;
    border:none;border-radius:10px;padding:12px 16px;cursor:pointer;
    transition:transform .1s, filter .15s;
  }
  button:active{transform:scale(.97);}
  .btn-primary{
    flex:1;background:linear-gradient(155deg,var(--amber),var(--amber-dim));color:#1c1204;
  }
  .btn-primary:hover{filter:brightness(1.08);}
  .btn-ghost{
    background:var(--panel-2);color:var(--text);border:1px solid var(--line);
  }
  .btn-ghost:hover{border-color:var(--amber);}

  .hint{
    font-size:12px;color:var(--muted);line-height:1.9;margin-top:16px;
    background:var(--panel-2);border:1px dashed var(--line);border-radius:10px;padding:12px 14px;
  }
  .hint b{color:var(--text);}

  /* preview column */
  .preview-panel{
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:16px;
    padding:22px;
    display:flex;flex-direction:column;
  }
  .preview-head{
    display:flex;justify-content:space-between;align-items:center;margin-bottom:16px;
  }
  .preview-head h2{
    font-size:12px;text-transform:uppercase;letter-spacing:1.2px;color:var(--amber);
    margin:0;font-weight:700;display:flex;align-items:center;gap:8px;
  }
  .preview-head h2::before{content:'';width:14px;height:2px;background:var(--amber);display:inline-block;}
  .size-tag{
    font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--muted);
    background:var(--panel-2);padding:5px 10px;border-radius:999px;border:1px solid var(--line);
  }

  .stage{
    flex:1;
    background:
      linear-gradient(var(--panel-2) 1px, transparent 1px),
      linear-gradient(90deg, var(--panel-2) 1px, transparent 1px);
    background-size:20px 20px;
    border-radius:12px;
    display:flex;align-items:center;justify-content:center;
    padding:40px 20px;
    min-height:320px;
  }

  .label-sheet{
    background:var(--paper);
    color:#141821;
    border-radius:3px;
    box-shadow:0 12px 30px rgba(0,0,0,.45);
    position:relative;
    display:flex;flex-direction:column;align-items:center;justify-content:center;
    padding:10px 8px;
    overflow:hidden;
  }
  .label-sheet::before, .label-sheet::after{
    content:'';position:absolute;left:0;right:0;height:1px;
    background-image:linear-gradient(90deg, var(--paper-line) 60%, transparent 0%);
    background-size:8px 1px;background-repeat:repeat-x;
  }
  .label-sheet::before{top:5px;}
  .label-sheet::after{bottom:5px;}

  .lbl-name{
    font-weight:700;font-size:13px;text-align:center;
    max-width:100%;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
    margin-bottom:4px;
  }
  .lbl-sku{
    font-family:'JetBrains Mono',monospace;font-size:9.5px;color:#555;margin-bottom:2px;
    letter-spacing:.5px;
  }
  .lbl-svg-wrap svg{display:block;max-width:100%;}
  .lbl-code-text{
    font-family:'JetBrains Mono',monospace;font-weight:600;font-size:11px;
    letter-spacing:2px;margin-top:2px;
  }
  .no-price-badge{
    position:absolute;top:6px;left:6px;
    font-family:'JetBrains Mono',monospace;font-size:8px;
    color:var(--amber-dim);border:1px solid var(--amber-dim);
    border-radius:4px;padding:1px 5px;opacity:.55;letter-spacing:.5px;
  }

  .qty-note{
    text-align:center;margin-top:14px;font-size:12px;color:var(--muted);
    font-family:'JetBrains Mono',monospace;
  }

  /* ===== PRINT ===== */
  #printArea{display:none;}

  @media print{
    body *{visibility:hidden;}
    #printArea, #printArea *{visibility:visible;}
    #printArea{
      display:flex !important;
      flex-wrap:wrap;
      position:absolute;top:0;left:0;
      gap:0;
    }
    .print-label{
      background:#fff;
      display:flex;flex-direction:column;align-items:center;justify-content:center;
      box-sizing:border-box;
      page-break-inside:avoid;
    }
    .print-label .lbl-name{font-weight:700;text-align:center;overflow:hidden;white-space:nowrap;text-overflow:ellipsis;width:100%;}
    .print-label .lbl-sku{font-family:'JetBrains Mono',monospace;color:#000;}
    .print-label svg{display:block;}
    @page{ margin:0; }
  }
</style>
</head>
<body>
<div class="wrap">

  <header class="top">
    <div class="brand">
      <div class="mark">XP</div>
      <div>
        <h1>مولّد ملصقات الباركود</h1>
        <p>بدون سعر — جاهز للطباعة على طابعات XPRINTER الحرارية</p>
      </div>
    </div>
    <div class="status"><span class="dot"></span> جاهز للطباعة عبر متصفحك</div>
  </header>

  <div class="grid">

    <!-- CONTROLS -->
    <div class="panel">
      <h2>بيانات الملصق</h2>

      <label class="field">
        <span>اسم المنتج (اختياري)</span>
        <input type="text" id="productName" placeholder="مثال: قميص قطن أزرق" />
      </label>

      <label class="field">
        <span>قيمة الباركود</span>
        <input type="text" id="barcodeValue" placeholder="مثال: 6291041500213" value="6291041500213" />
      </label>

      <div class="row2">
        <label class="field">
          <span>نوع الترميز</span>
          <select id="barcodeFormat">
            <option value="CODE128" selected>CODE128 (عام)</option>
            <option value="EAN13">EAN-13</option>
            <option value="EAN8">EAN-8</option>
            <option value="UPC">UPC-A</option>
            <option value="CODE39">CODE39</option>
          </select>
        </label>
        <label class="field">
          <span>عدد النسخ</span>
          <input type="number" id="qty" min="1" max="200" value="1" />
        </label>
      </div>

      <label class="field">
        <span>مقاس الملصق (لطابعات XPRINTER)</span>
        <select id="labelSize">
          <option value="40x30">40 × 30 مم</option>
          <option value="50x30" selected>50 × 30 مم</option>
          <option value="58x40">58 × 40 مم</option>
          <option value="60x40">60 × 40 مم</option>
          <option value="76x50">76 × 50 مم</option>
          <option value="100x50">100 × 50 مم</option>
          <option value="58cont">لفة مستمرة 58 مم</option>
          <option value="80cont">لفة مستمرة 80 مم</option>
        </select>
      </label>

      <label class="field" style="margin-bottom:6px;">
        <span>خيارات العرض</span>
        <div style="display:flex;gap:16px;margin-top:4px;">
          <label style="display:flex;align-items:center;gap:6px;font-size:13px;color:var(--text);cursor:pointer;">
            <input type="checkbox" id="showText" checked style="width:auto;"> إظهار الرقم أسفل الباركود
          </label>
        </div>
      </label>

      <div class="btn-row">
        <button class="btn-primary" onclick="renderPreview()">تحديث المعاينة</button>
        <button class="btn-ghost" onclick="doPrint()">🖨 طباعة</button>
      </div>

      <div class="hint">
        <b>لربط طابعة XPRINTER الحرارية:</b> ثبّت تعريف الطابعة (USB أو شبكة) في نظام التشغيل حتى تظهر كطابعة عادية، ثم عند الضغط على «طباعة» اختر اسم طابعة XPRINTER من نافذة الطباعة، وعطّل الهوامش (Margins → None) واختر «حجم الورق المخصص» مطابقًا لمقاس الملصق أعلاه.
      </div>
    </div>

    <!-- PREVIEW -->
    <div class="preview-panel">
      <div class="preview-head">
        <h2>معاينة الملصق</h2>
        <span class="size-tag" id="sizeTag">50×30mm</span>
      </div>

      <div class="stage">
        <div class="label-sheet" id="labelSheet">
          <span class="no-price-badge">NO PRICE</span>
          <div class="lbl-name" id="pName">اسم المنتج</div>
          <div class="lbl-svg-wrap"><svg id="barcodeSvg"></svg></div>
        </div>
      </div>

      <div class="qty-note" id="qtyNote">سيتم طباعة نسخة واحدة</div>
    </div>

  </div>
</div>

<!-- hidden print composition -->
<div id="printArea"></div>

<script>
const sizeMap = {
  "40x30": {w:40, h:30, roll:false},
  "50x30": {w:50, h:30, roll:false},
  "58x40": {w:58, h:40, roll:false},
  "60x40": {w:60, h:40, roll:false},
  "76x50": {w:76, h:50, roll:false},
  "100x50":{w:100,h:50, roll:false},
  "58cont":{w:58, h:30, roll:true},
  "80cont":{w:80, h:30, roll:true},
};

function mmToPx(mm){ return mm * 3.7795275591; } // approx 96dpi

function currentConfig(){
  const name = document.getElementById('productName').value.trim();
  const value = document.getElementById('barcodeValue').value.trim() || '0000000000000';
  const format = document.getElementById('barcodeFormat').value;
  const showText = document.getElementById('showText').checked;
  const qty = Math.max(1, Math.min(200, parseInt(document.getElementById('qty').value || '1', 10)));
  const sizeKey = document.getElementById('labelSize').value;
  const size = sizeMap[sizeKey];
  return {name, value, format, showText, qty, size, sizeKey};
}

function safeBarcode(svgEl, value, format, showText, widthPx){
  try{
    JsBarcode(svgEl, value, {
      format: format,
      displayValue: showText,
      fontSize: 12,
      fontOptions: "600",
      font: "JetBrains Mono",
      margin: 0,
      height: Math.max(28, widthPx*0.28),
      width: 2,
      textMargin: 3,
    });
    return true;
  }catch(e){
    JsBarcode(svgEl, "0000000000000", {format:"CODE128", displayValue:true, margin:0, height:34});
    return false;
  }
}

function renderPreview(){
  const cfg = currentConfig();
  document.getElementById('sizeTag').textContent =
    cfg.size.roll ? `${cfg.size.w}mm (مستمر)` : `${cfg.size.w}×${cfg.size.h}mm`;

  const sheet = document.getElementById('labelSheet');
  const pxW = mmToPx(cfg.size.w);
  const pxH = cfg.size.roll ? mmToPx(45) : mmToPx(cfg.size.h);
  sheet.style.width = pxW + 'px';
  sheet.style.height = pxH + 'px';

  const pName = document.getElementById('pName');
  if(cfg.name){
    pName.textContent = cfg.name;
    pName.style.display = 'block';
  } else {
    pName.style.display = 'none';
  }

  const svg = document.getElementById('barcodeSvg');
  svg.innerHTML = '';
  safeBarcode(svg, cfg.value, cfg.format, cfg.showText, pxW);

  document.getElementById('qtyNote').textContent =
    cfg.qty === 1 ? 'سيتم طباعة نسخة واحدة' : `سيتم طباعة ${cfg.qty} نسخة`;
}

function buildPrintArea(){
  const cfg = currentConfig();
  const area = document.getElementById('printArea');
  area.innerHTML = '';

  const wMm = cfg.size.w, hMm = cfg.size.roll ? 30 : cfg.size.h;

  // set page size for the browser print dialog
  let styleTag = document.getElementById('dynamicPageSize');
  if(!styleTag){
    styleTag = document.createElement('style');
    styleTag.id = 'dynamicPageSize';
    document.head.appendChild(styleTag);
  }
  styleTag.textContent = `@page{ size:${wMm}mm ${hMm}mm; margin:0; }`;

  for(let i=0;i<cfg.qty;i++){
    const lbl = document.createElement('div');
    lbl.className = 'print-label';
    lbl.style.width = wMm + 'mm';
    lbl.style.height = hMm + 'mm';
    lbl.style.padding = '1.5mm';

    if(cfg.name){
      const nameEl = document.createElement('div');
      nameEl.className = 'lbl-name';
      nameEl.style.fontSize = '3mm';
      nameEl.style.marginBottom = '0.8mm';
      nameEl.textContent = cfg.name;
      lbl.appendChild(nameEl);
    }

    const svgNS = "http://www.w3.org/2000/svg";
    const svg = document.createElementNS(svgNS, "svg");
    safeBarcode(svg, cfg.value, cfg.format, cfg.showText, mmToPx(wMm));
    lbl.appendChild(svg);

    area.appendChild(lbl);
  }
}

function doPrint(){
  renderPreview();
  buildPrintArea();
  setTimeout(()=>window.print(), 60);
}

// live updates
['productName','barcodeValue','barcodeFormat','labelSize','showText','qty'].forEach(id=>{
  document.getElementById(id).addEventListener('input', renderPreview);
  document.getElementById(id).addEventListener('change', renderPreview);
});

renderPreview();
</script>

</body>
</html>
